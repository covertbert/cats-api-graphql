name: Branch Pipeline

on:
  push:
    branches-ignore: master

jobs:
  # lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v1
  #     - name: Install
  #       run: npm ci
  #     - name: Prettier
  #       run: npm run prettier
  #     - name: Lint
  #       run: npm run lint
  # test:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v1
  #     - name: Install
  #       run: npm ci
  #     - name: Docker
  #       run: cd ops/docker; ./run-ci.sh
  #     - name: Test
  #       run: npm run test
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        uses: docker://covertbert/google-cloud-sdk:latest
        with:
          args: gcloud builds submit --tag gcr.io/cats-graphql-api/cats
